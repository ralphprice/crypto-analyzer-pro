# Bind to localhost only (prevents external access)
bind 127.0.0.1  
# Enables protected mode to block non-local connections
protected-mode yes
# Set a password (optional but recommended for production)
requirepass 1234567  
# Log to file for easier monitoring
logfile /home/ralph/crypto-analyzer-pro/var/log/redis/redis.log
